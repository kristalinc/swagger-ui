<label for="parameterContentType" class="c-help-text"></label>
<select name="parameterContentType">
{{#if consumes}}
  {{#each consumes}}
  <option value="{{{this}}}">{{{this}}}</option>
  {{/each}}
{{else}}
  <option value="application/json">application/json</option>
{{/if}}
</select>
